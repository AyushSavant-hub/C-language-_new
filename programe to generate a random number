#include <stdio.h>
#include<stdlib.h>
#include<time.h>
int main() {
    srand(time(NULL));
    int random_num=(rand()%900)+100;
    printf("the generated random number is %d",random_num);
    return 0;
}
